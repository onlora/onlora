# Frontend Configuration

# Base URL for the backend API
# In docker-compose, this is typically http://backend:8080 (service name)
# For local development outside docker, might be http://localhost:8080 (or backend port)
# For production, set to the deployed backend URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Add other NEXT_PUBLIC_ variables required by the frontend build/runtime below
# These variables are embedded during the build process or available at runtime.
# Example:
# NEXT_PUBLIC_ANOTHER_KEY=value 